var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-reference","page":"API reference","text":"LibPARI exposes PARI/GP to Julia through two layers: a large machine-generated binding layer, and a compact hand-written core. This page documents both.","title":"API reference"},{"category":"section","location":"api/#The-generated-bindings-—-LibPARI.PARI","page":"API reference","text":"The LibPARI.PARI submodule holds nearly 1200 bindings, one per eligible PARI/GP function, generated directly from PARI's own machine-readable function database (pari.desc). Each binding:\n\nkeeps PARI's function name (e.g. LibPARI.PARI.nextprime, LibPARI.PARI.factorial, LibPARI.PARI.isprime);\ncarries PARI's own Help text as its docstring — query it with ? at the REPL, e.g. ?LibPARI.PARI.nextprime;\ntakes and returns Gen values (or the scalar Julia type the function's prototype calls for), and is type-stable;\nroutes through the same safe, leak-free, thread-safe call boundary as the hand-written API.\n\nA few prototype conventions are worth knowing:\n\nPrecision arguments are supplied automatically; pass a prec keyword to override the working precision.\nOptional arguments are exposed as keyword arguments with PARI's declared defaults.\nOutput arguments are returned alongside the primary result as a tuple.\nFunctions taking a GP closure argument are not generated — reach them through gp_eval instead.\n\nBecause the binding layer is generated, it is not enumerated symbol-by-symbol here; PARI's own function reference is the authoritative index of what each function computes. The rest of this page documents the hand-written core in full.","title":"The generated bindings — LibPARI.PARI"},{"category":"section","location":"api/#Hand-written-core","page":"API reference","text":"Pages = [\"api.md\"]","title":"Hand-written core"},{"category":"section","location":"api/#Library-lifecycle","page":"API reference","text":"","title":"Library lifecycle"},{"category":"section","location":"api/#The-Gen-value-type","page":"API reference","text":"","title":"The Gen value type"},{"category":"section","location":"api/#Error-handling","page":"API reference","text":"","title":"Error handling"},{"category":"section","location":"api/#The-entry-point-and-the-high-level-facade","page":"API reference","text":"pari(x) is the entry point; the rest of this section is the deliberately small facade over the generated layer. Every facade function documents the Julia and PARI inputs it accepts, its exact return type, and how it fails.","title":"The entry point and the high-level facade"},{"category":"section","location":"api/#Type-conversions","page":"API reference","text":"LibPARI converts an enumerated set of Julia types, not every Number. The set is LibPARI.PariConvertible and the single entry point is LibPARI.gen_convert; Gen(x), convert(Gen, x) and every mixed-operand operator funnel through it, so one type is converted in exactly one way.\n\nInto a Gen:\n\nJulia type PARI type Notes\nBool t_INT 0/1 — PARI has no boolean type, and Bool <: Integer\nInt8 … Int128, UInt8 … UInt128 t_INT exact at every width\nBigInt t_INT exact, any magnitude\nRational{<:Integer} t_FRAC reduced by PARI; an integral value normalises to t_INT\nFloat16, Float32, Float64 t_REAL exact\nBigFloat t_REAL exact — see Precision\nComplex{T}, T convertible t_COMPLEX a zero imaginary part normalises to the real type\n\nAnything else — an Irrational such as π, a Missing, a foreign numeric type — raises LibPARI.ConversionError, which names the offending type. A downstream package adds support for its own type by defining a LibPARI.gen_convert method for it, which is type piracy on neither side.\n\nInf, -Inf and NaN raise InexactError: PARI's t_INFINITY exists but does not take part in general arithmetic, so mapping onto it would produce values that fail later, far from the conversion.\n\nOut of a Gen: BigInt, Bool and the fixed-width integer types (InexactError when the value is not an integer or does not fit); Rational and Rational{T}; Float16, Float32, Float64 and BigFloat; Complex and Complex{T}. A t_REAL is decomposed with PARI's own mantissa and exponent rather than re-parsed from its printed form, so the conversion neither loses digits nor fails on a large exponent.","title":"Type conversions"},{"category":"section","location":"api/#Numeric-API","page":"API reference","text":"Gen is a PariObject, not a Julia Number — one concrete Gen wraps every PARI object, matrices and strings included. The numeric surface is therefore declared explicitly, not inherited: +, -, *, /, ^, \\, ==, <, <= and isless have methods for Gen/Gen and for a Gen against a Julia Integer, AbstractFloat, Rational or Complex, in either operand order. Gen also carries the zero/one identities, the standard predicates (iszero, isinteger, isfinite, …), the elementary operations (abs, sign, inv, conj, real, imag), hash, float, abs2, adjoint, transpose and scalar broadcasting — so a Gen sorts and serves as a Dict/Set key interchangeably with an equal Julia number.\n\nGeneric code bounded by T<:Number — parts of LinearAlgebra, other numeric packages — does not accept a Gen; dispatch on PariObject, or convert. An operation that does not apply to a Gen's underlying PARI type raises a catchable PariError.","title":"Numeric API"},{"category":"section","location":"api/#GP-expression-evaluator","page":"API reference","text":"","title":"GP expression evaluator"},{"category":"section","location":"api/#Module","page":"API reference","text":"","title":"Module"},{"category":"module","location":"api/#LibPARI.LibraryState","page":"API reference","text":"Lifecycle states of the embedded PARI library.\n\nLibraryState.T is a module-scoped enum with three values:\n\nUNINITIALIZED — pari_init has not (successfully) run\nINITIALIZED   — PARI is ready; a process-exit shutdown is registered\nCLOSED        — pari_close has run at process exit\n\n\n\n\n\n","title":"LibPARI.LibraryState"},{"category":"method","location":"api/#LibPARI._close_libpari!-Tuple{}","page":"API reference","text":"Release PARI's resources by calling pari_close, but only while the library is INITIALIZED. Idempotent — a second invocation is a no-op. Registered with atexit so it runs once at process exit (REQ-INI-05).\n\n\n\n\n\n","title":"LibPARI._close_libpari!"},{"category":"method","location":"api/#LibPARI._configured_stack_size-Tuple{}","page":"API reference","text":"Return the PARI main-stack size (bytes) for this process: the value of the LIBPARI_STACK_SIZE environment variable when set, otherwise the 8 MiB default. Throw ArgumentError when the variable holds an invalid value.\n\n\n\n\n\n","title":"LibPARI._configured_stack_size"},{"category":"method","location":"api/#LibPARI._init_libpari!-Tuple{Integer, Integer}","page":"API reference","text":"Initialize the PARI library for this process.\n\nInitializes PARI exactly once: when the library is not UNINITIALIZED this is a no-op (the guard for REQ-INI-02 / REQ-INI-06). parisize is validated first; an invalid value throws before PARI is initialized, leaving the state UNINITIALIZED (REQ-INI-07). On success the state becomes INITIALIZED, the size is recorded, and pari_close is registered to run at process exit.\n\nUses pari_init_opts (not plain pari_init) with INIT_SIGm cleared, so PARI leaves Julia's signal handlers intact (research.md D10).\n\n\n\n\n\n","title":"LibPARI._init_libpari!"},{"category":"method","location":"api/#LibPARI._validate_parisize-Tuple{Integer}","page":"API reference","text":"Validate a candidate PARI main-stack size n (bytes) and return it as an Int. Throw ArgumentError when n is below the 1 MiB minimum.\n\n\n\n\n\n","title":"LibPARI._validate_parisize"},{"category":"method","location":"api/#LibPARI.is_initialized-Tuple{}","page":"API reference","text":"is_initialized() -> Bool\n\n\nReturn true when the PARI library has been successfully initialized in this process and not yet closed.\n\nExamples\n\njulia> using LibPARI\n\njulia> LibPARI.is_initialized()\ntrue\n\n\n\n\n\n","title":"LibPARI.is_initialized"},{"category":"method","location":"api/#LibPARI.library_state-Tuple{}","page":"API reference","text":"library_state() -> LibPARI.LibraryState.T\n\n\nReturn the current lifecycle state of the PARI library as a LibraryState.T value (UNINITIALIZED, INITIALIZED, or CLOSED).\n\nExamples\n\njulia> using LibPARI\n\njulia> LibPARI.library_state() === LibPARI.LibraryState.INITIALIZED\ntrue\n\n\n\n\n\n","title":"LibPARI.library_state"},{"category":"method","location":"api/#LibPARI.stack_size-Tuple{}","page":"API reference","text":"stack_size() -> Int64\n\n\nReturn the size, in bytes, of PARI's main stack — the value passed to pari_init for this process (the 8 MiB default, or a LIBPARI_STACK_SIZE override). Returns 0 before initialization.\n\nExamples\n\njulia> using LibPARI\n\njulia> LibPARI.stack_size() isa Integer\ntrue\n\njulia> LibPARI.stack_size() >= 1024 * 1024\ntrue\n\n\n\n\n\n","title":"LibPARI.stack_size"},{"category":"type","location":"api/#LibPARI.Gen","page":"API reference","text":"mutable struct Gen <: LibPARI.PariObject\n\nA Julia value wrapping one PARI object (a PARI GEN).\n\nGen is a concrete, mutable type. Each Gen owns a private clone of its PARI object in PARI's persistent storage; a garbage-collection finalizer frees that clone exactly once. Every PARI object that LibPARI exposes to Julia is a Gen.\n\nGen is a PariObject, not a Julia Number: one concrete Gen wraps every PARI type, matrices, strings and closures included, so a Number supertype would be a false claim (REQ-TYPE-01). The arithmetic, equality and ordering that Base's Number fallbacks used to supply are declared explicitly on Gen instead — see src/numeric.jl.\n\nExamples\n\njulia> using LibPARI\n\njulia> isconcretetype(LibPARI.Gen)\ntrue\n\n\n\n\n\n","title":"LibPARI.Gen"},{"category":"type","location":"api/#LibPARI.PariObject","page":"API reference","text":"abstract type PariObject\n\nThe supertype of every PARI object LibPARI exposes to Julia.\n\nPariObject exists so that Gen can name what it is. A Gen wraps any PARI GEN — an integer, a real, a matrix, a string, a closure — so it cannot honestly be a Julia Number (REQ-TYPE-01). Julia code that must accept a PARI object generically dispatches on PariObject.\n\nExamples\n\njulia> using LibPARI\n\njulia> LibPARI.Gen <: LibPARI.PariObject\ntrue\n\n\n\n\n\n","title":"LibPARI.PariObject"},{"category":"method","location":"api/#LibPARI.gen_from-Tuple{Any}","page":"API reference","text":"Build a Gen from producer, a function that returns a raw PARI GEN computed on the transient stack.\n\ngen_from captures PARI's stack pointer, runs producer, clones the result into persistent storage, restores the stack pointer, and returns the Gen. This capture → produce → clone → restore discipline keeps the PARI stack leak-free (REQ-MEM-03, REQ-MEM-05); it is the pattern every generated binding (M4) will reuse.\n\nExamples\n\njulia> using LibPARI\n\njulia> LibPARI.gen_from isa Function\ntrue\n\n\n\n\n\n","title":"LibPARI.gen_from"},{"category":"method","location":"api/#LibPARI.gentype-Tuple{Gen}","page":"API reference","text":"gentype(g::Gen) -> LibPARI.PariType.T\n\n\nReturn the PARI value type of g as a PariType.T — T_INT, T_REAL, T_POL, T_VEC, and so on.\n\nExamples\n\njulia> using LibPARI\n\njulia> LibPARI.gentype isa Function\ntrue\n\n\n\n\n\n","title":"LibPARI.gentype"},{"category":"module","location":"api/#LibPARI.PariType","page":"API reference","text":"Module-scoped enum of PARI's value types — the t_* type tags.\n\nPariType.T has one value per PARI object type (T_INT, T_REAL, T_POL, T_VEC, …). Each value's integer equals PARI's own t_* constant, so a type tag read from a GEN converts directly to PariType.T. See gentype.\n\n\n\n\n\n","title":"LibPARI.PariType"},{"category":"module","location":"api/#LibPARI.PariErr","page":"API reference","text":"Module-scoped enum of PARI's error categories (PARI's numerr_t).\n\nPariErr.T has one value per PARI error kind — e_SYNTAX, e_TYPE, e_DOMAIN, e_INV, e_STACK, and so on. Each value's integer equals PARI's own error number. See gentype's sibling PariError for how it is used.\n\n\n\n\n\n","title":"LibPARI.PariErr"},{"category":"type","location":"api/#LibPARI.PariError","page":"API reference","text":"struct PariError <: Exception\n\nException raised when a libpari call signals a PARI error.\n\nPariError carries PARI's own error message and a category (a PariErr.T naming the kind of error). It is an ordinary catchable Julia exception — use try/catch.\n\nExamples\n\njulia> using LibPARI\n\njulia> LibPARI.PariError <: Exception\ntrue\n\n\n\n\n\n","title":"LibPARI.PariError"},{"category":"method","location":"api/#LibPARI.protected_call-Tuple{Any}","page":"API reference","text":"Run producer — a function performing one or more libpari calls — inside the PARI error-trapping boundary.\n\nOn success the result of producer() is returned unchanged. If a libpari call raises a PARI error, a PariError is thrown; the PARI stack pointer is restored to its pre-call value before the exception propagates, so the library stays usable. This is the boundary every generated binding (M4) routes through.\n\nExamples\n\njulia> using LibPARI\n\njulia> LibPARI.protected_call isa Function\ntrue\n\n\n\n\n\n","title":"LibPARI.protected_call"},{"category":"method","location":"api/#Base.denominator-Tuple{Gen}","page":"API reference","text":"denominator(g::Gen) -> Gen\n\n\nThe denominator of an integer or rational Gen; 1 for an integer.\n\nThrows ArgumentError for any other PARI type — see numerator.\n\n\n\n\n\n","title":"Base.denominator"},{"category":"method","location":"api/#Base.factorial-Tuple{Gen}","page":"API reference","text":"factorial(g::Gen) -> Gen\n\n\nThe factorial of a non-negative integer Gen, computed by PARI.\n\nThrows ArgumentError for a negative or non-integer argument — PARI's own mpfact is not checked for a negative input, so the wrapper validates first.\n\nExamples\n\njulia> using LibPARI\n\njulia> factorial(pari(20))\n2432902008176640000\n\n\n\n\n\n","title":"Base.factorial"},{"category":"method","location":"api/#Base.gcd-Tuple{Gen, Gen}","page":"API reference","text":"gcd(a::Gen, b::Gen) -> Gen\n\n\nThe greatest common divisor, as PARI computes it.\n\nAccepts an integer- or rational-valued Gen, or any PariConvertible value of those kinds, on either side; returns a Gen. Matches Base.gcd, which is defined for Integer and Rational.\n\nThrows ArgumentError on any other PARI type. PARI answers for reals, complex numbers and polynomials too — gcd(12, 1.5) is 1 there — but those are PARI's domain, not Julia's; reach them as LibPARI.PARI.ggcd0.\n\nExamples\n\njulia> using LibPARI\n\njulia> gcd(pari(12), 18)\n6\n\n\n\n\n\n","title":"Base.gcd"},{"category":"method","location":"api/#Base.gcdx-Tuple{Gen, Gen}","page":"API reference","text":"gcdx(a::Gen, b::Gen) -> Tuple{Gen, Gen, Gen}\n\n\nThe extended greatest common divisor: (d, u, v) with d == u*a + v*b, in Julia's argument order.\n\nPARI's gcdext returns [u, v, d]; this reorders it to match Base.gcdx so the result can be destructured the usual way.\n\nRestricted to integer- and rational-valued arguments, like gcd — PARI computes a Bézout identity over the reals too, which Base.gcdx does not mean.\n\nExamples\n\njulia> using LibPARI\n\njulia> d, u, v = gcdx(pari(12), pari(18));\n\njulia> d == u * pari(12) + v * pari(18)\ntrue\n\n\n\n\n\n","title":"Base.gcdx"},{"category":"method","location":"api/#Base.numerator-Tuple{Gen}","page":"API reference","text":"numerator(g::Gen) -> Gen\n\n\nThe numerator of an integer or rational Gen.\n\nThrows ArgumentError for any other PARI type: PARI answers denominator(x/2 + 1/3) == 1 in the polynomial domain, which is a different question from Julia's numerator.\n\n\n\n\n\n","title":"Base.numerator"},{"category":"method","location":"api/#LibPARI.factor-Tuple{Gen}","page":"API reference","text":"factor(n::Gen) -> Gen\n\n\nThe factorization of a non-zero integer Gen, in PARI's own shape: a two-column t_MAT of primes and exponents.\n\nA negative argument carries the unit -1 as its first factor, as it does in Primes.jl. Zero raises ArgumentError: it has no factorization, and PARI's own answer for it ([0 1]) does not name a prime.\n\nSee factors for the Julia shape.\n\n\n\n\n\n","title":"LibPARI.factor"},{"category":"method","location":"api/#LibPARI.factors-Tuple{Gen}","page":"API reference","text":"factors(n::Gen) -> Vector{Pair{Gen, Gen}}\n\n\nThe factorization of an integer as a Vector{Pair{Gen,Gen}} of prime => exponent, in increasing order of prime.\n\nReturns an empty vector for 1, which has no prime factors. A negative argument carries -1 => 1 first. Zero raises ArgumentError.\n\nExamples\n\njulia> using LibPARI\n\njulia> LibPARI.factors(60)\n3-element Vector{Pair{Gen, Gen}}:\n 2 => 2\n 3 => 1\n 5 => 1\n\n\n\n\n\n","title":"LibPARI.factors"},{"category":"method","location":"api/#LibPARI.isprime-Tuple{Gen}","page":"API reference","text":"isprime(n::Gen) -> Bool\n\n\nWhether n is prime, as a Julia Bool.\n\nAccepts a Gen or any PariConvertible integer. Uses PARI's isprime, which is a proof, not a probabilistic test.\n\nNot exported: the name belongs to Primes.jl.\n\nExamples\n\njulia> using LibPARI\n\njulia> LibPARI.isprime(1009)\ntrue\n\n\n\n\n\n","title":"LibPARI.isprime"},{"category":"method","location":"api/#LibPARI.nextprime-Tuple{Gen}","page":"API reference","text":"nextprime(n::Gen) -> Gen\n\n\nThe smallest prime >= n.\n\nPARI documents this as the next pseudoprime; for the sizes reachable here it is a proven prime.\n\n\n\n\n\n","title":"LibPARI.nextprime"},{"category":"method","location":"api/#LibPARI.pari-Tuple{Any}","page":"API reference","text":"pari(x) -> Any\n\n\nConvert x to a PARI value — the entry point to LibPARI.\n\npari accepts the Julia types listed in PariConvertible: integers of any size, rationals, floats (including BigFloat, exactly), and complex numbers of those. A Gen is returned unchanged, so pari is cheap to apply defensively and never clones twice.\n\nAnything else raises ConversionError naming the type.\n\nExamples\n\njulia> using LibPARI\n\njulia> pari(42)\n42\n\njulia> pari(3 // 4) + 1\n7/4\n\njulia> g = pari(2)^100\n1267650600228229401496703205376\n\njulia> pari(g) === g\ntrue\n\n\n\n\n\n","title":"LibPARI.pari"},{"category":"method","location":"api/#LibPARI.prevprime-Tuple{Gen}","page":"API reference","text":"prevprime(n::Gen) -> Gen\n\n\nThe largest prime <= n.\n\n\n\n\n\n","title":"LibPARI.prevprime"},{"category":"type","location":"api/#LibPARI.PariConvertible","page":"API reference","text":"The Julia types LibPARI guarantees it can convert to a Gen.\n\nPariConvertible is an enumerated set, deliberately not Number: a rule that accepted every Number would promise conversions LibPARI cannot perform, and the failure would surface far from the call that caused it.\n\nSupport a further type by adding a method to gen_convert — that is a method on your own type, so it is not type piracy on either side.\n\n\n\n\n\n","title":"LibPARI.PariConvertible"},{"category":"method","location":"api/#Base.Complex-Tuple{Gen}","page":"API reference","text":"Complex(g::Gen) -> ComplexF64\n\n\nConvert a numeric Gen to a Julia Complex{Float64} — the real and imaginary parts of g as Float64s.\n\n\n\n\n\n","title":"Base.Complex"},{"category":"method","location":"api/#Base.Complex-Union{Tuple{Gen}, Tuple{T}} where T<:Real","page":"API reference","text":"Complex(g::Gen) -> ComplexF64\n\n\nConvert a numeric Gen to a Complex{T} — its real and imaginary parts each converted to T (REQ-PROM-08).\n\n\n\n\n\n","title":"Base.Complex"},{"category":"method","location":"api/#Base.GMP.BigInt-Tuple{Gen}","page":"API reference","text":"BigInt(g::Gen) -> BigInt\n\n\nConvert an integer-valued Gen to an exact Julia BigInt.\n\nThrows InexactError when g is not an integer — a fraction, a real with a fractional part, or a non-numeric value.\n\nExamples\n\njulia> using LibPARI\n\njulia> BigInt(LibPARI.Gen(-123456789))\n-123456789\n\n\n\n\n\n","title":"Base.GMP.BigInt"},{"category":"method","location":"api/#Base.MPFR.BigFloat-Tuple{Gen}","page":"API reference","text":"BigFloat(g::Gen; precision) -> BigFloat\n\n\nConvert a real-valued Gen to a Julia BigFloat.\n\nWith no precision keyword a t_REAL converts exactly: the result carries PARI's own mantissa, at whatever precision that takes — which may exceed the ambient precision(BigFloat). Pass precision to round once to a chosen width instead (REQ-PREC-07).\n\nThrows InexactError when g is not a real number.\n\nExamples\n\njulia> using LibPARI\n\njulia> precision(BigFloat(LibPARI.Gen(1.5); precision = 256))\n256\n\n\n\n\n\n","title":"Base.MPFR.BigFloat"},{"category":"method","location":"api/#Base.Rational-Tuple{Gen}","page":"API reference","text":"Rational(g::Gen) -> Rational{BigInt}\n\n\nConvert a rational- or integer-valued Gen to a Julia Rational{BigInt}.\n\nThrows InexactError when g is not a rational number.\n\n\n\n\n\n","title":"Base.Rational"},{"category":"method","location":"api/#Base.Rational-Union{Tuple{Gen}, Tuple{T}} where T<:Integer","page":"API reference","text":"Rational(g::Gen) -> Rational{BigInt}\n\n\nConvert a rational- or integer-valued Gen to a Rational{T}.\n\nThrows InexactError when g is not rational, or when its numerator or denominator lies outside the range of T (REQ-PROM-08).\n\n\n\n\n\n","title":"Base.Rational"},{"category":"method","location":"api/#Core.Float32-Tuple{Gen}","page":"API reference","text":"Float32(g::Gen) -> Float32\n\n\nConvert a real-valued Gen to a narrower Julia float — Float16 or Float32.\n\nThe value is read at full PARI precision and rounded once to F (REQ-PROM-08).\n\n\n\n\n\n","title":"Core.Float32"},{"category":"method","location":"api/#Core.Float64-Tuple{Gen}","page":"API reference","text":"Float64(g::Gen) -> Float64\n\n\nConvert a real-valued Gen to a Julia Float64.\n\nA PARI real too large for Float64 converts to ±Inf, as any other Julia floating-point conversion would.\n\nThrows InexactError when g is not a real number (a complex value, a polynomial, a vector, …).\n\n\n\n\n\n","title":"Core.Float64"},{"category":"method","location":"api/#Core.Type-Union{Tuple{Gen}, Tuple{T}} where T<:Union{Int128, Int16, Int32, Int64, Int8, UInt128, UInt16, UInt32, UInt64, UInt8}","page":"API reference","text":"Convert an integer-valued Gen to a fixed-width Julia integer type T.\n\nT is one of Julia's built-in integer types (Base.BitInteger), BigInt or Bool — the set LibPARI can validate. A third-party Integer subtype still works, through T(BigInt(g)), but warns: LibPARI cannot check that its construction from a BigInt is exact (REQ-PROM-09).\n\nThrows InexactError when g is not an integer, or when its value lies outside the range of T.\n\nExamples\n\njulia> using LibPARI\n\njulia> Int(LibPARI.Gen(255))\n255\n\n\n\n\n\n","title":"Core.Type"},{"category":"type","location":"api/#LibPARI.ConversionError","page":"API reference","text":"struct ConversionError <: Exception\n\nRaised when a value cannot be converted to a Gen.\n\nCarries the offending type and the reason, and names gen_convert — the extension point — rather than surfacing as a MethodError inside a Base function the caller never invoked.\n\n\n\n\n\n","title":"LibPARI.ConversionError"},{"category":"method","location":"api/#LibPARI.Gen-Tuple{Integer}","page":"API reference","text":"Gen(x::Integer) -> Any\n\n\nConvert a Julia Integer of any magnitude to an integer-valued Gen (a PARI t_INT).\n\nExamples\n\njulia> using LibPARI\n\njulia> LibPARI.Gen(42) isa LibPARI.Gen\ntrue\n\n\n\n\n\n","title":"LibPARI.Gen"},{"category":"method","location":"api/#LibPARI.gen_convert-Tuple{Gen}","page":"API reference","text":"gen_convert(g::Gen) -> Gen\n\n\nConvert x to a Gen — the single conversion entry point.\n\nEvery hand-written conversion, every mixed-operand arithmetic method and every convert(Gen, x) funnels through here, so one type is converted in exactly one way. A Gen argument is returned unchanged, never re-cloned.\n\nA value outside PariConvertible, or one whose conversion fails, raises a ConversionError naming the type — never a MethodError from inside a Base function the caller did not invoke.\n\nExamples\n\njulia> using LibPARI\n\njulia> LibPARI.gen_convert(42) == LibPARI.Gen(42)\ntrue\n\n\n\n\n\n","title":"LibPARI.gen_convert"},{"category":"method","location":"api/#LibPARI.Gen-Tuple{Complex}","page":"API reference","text":"Gen(z::Complex) -> Gen\n\n\nConvert a Julia Complex number to a Gen (a PARI complex value).\n\nExamples\n\njulia> using LibPARI\n\njulia> LibPARI.Gen(3 + 4im) isa LibPARI.Gen\ntrue\n\n\n\n\n\n","title":"LibPARI.Gen"},{"category":"method","location":"api/#LibPARI.Gen-Tuple{Rational}","page":"API reference","text":"Gen(x::Rational) -> Any\n\n\nConvert a Julia Rational to a Gen (a reduced PARI rational value).\n\nExamples\n\njulia> using LibPARI\n\njulia> LibPARI.Gen(3 // 4) isa LibPARI.Gen\ntrue\n\n\n\n\n\n","title":"LibPARI.Gen"},{"category":"method","location":"api/#LibPARI.gp_eval-Tuple{AbstractString}","page":"API reference","text":"gp_eval(s::AbstractString) -> Any\n\n\nEvaluate a GP-language expression string with PARI's GP engine and return the result as a Gen.\n\ngp_eval reaches every PARI capability — including the GP-closure-argument functions (sum, intnum, …) that the generated bindings do not expose. A syntactically invalid string raises a catchable PariError describing the parse error; a runtime failure raises a PariError too. GP variable assignments persist across calls — gp_eval shares one GP environment.\n\nExamples\n\njulia> using LibPARI\n\njulia> LibPARI.gp_eval(\"2 + 2\") isa LibPARI.Gen\ntrue\n\n\n\n\n\n","title":"LibPARI.gp_eval"},{"category":"module","location":"api/#LibPARI.LibPARI","page":"API reference","text":"LibPARI\n\nA Julia wrapper for the PARI/GP number-theory library.\n\nLoading this module initializes the embedded PARI library once for the process and registers a clean shutdown at process exit. LibPARI exposes PARI/GP in two ways: nearly 1200 functions generated from PARI's own function database, reachable through the LibPARI.PARI submodule, and a hand-written core — the Gen value type, idiomatic numeric operators, conversions to and from Julia numbers, the gp_eval expression evaluator, and safe library lifecycle and error handling.\n\nThe lifecycle is observable through LibPARI.is_initialized, LibPARI.library_state, and LibPARI.stack_size.\n\n\n\n\n\n","title":"LibPARI.LibPARI"},{"category":"method","location":"api/#LibPARI.__init__-Tuple{}","page":"API reference","text":"Initialize PARI when the module is loaded into a process (REQ-INI-02, REQ-INI-03). Runs once, never during precompilation; reads the optional LIBPARI_STACK_SIZE configuration and delegates to _init_libpari!.\n\n\n\n\n\n","title":"LibPARI.__init__"},{"category":"section","location":"getting-started/#Getting-started","page":"Getting started","text":"","title":"Getting started"},{"category":"section","location":"getting-started/#Installation","page":"Getting started","text":"LibPARI is installable from the Julia General registry:\n\nusing Pkg\nPkg.add(\"LibPARI\")\n\nThe native PARI library is supplied by PARI_jll, a registered binary build of PARI 2.17.3 for Linux, macOS, and Windows. Pkg.add resolves and installs it automatically — no system PARI is required.\n\nLibPARI requires Julia 1.10 (the long-term-support release) or later.","title":"Installation"},{"category":"section","location":"getting-started/#Loading-the-package","page":"Getting started","text":"using LibPARI\n\nLoading the module initializes the embedded PARI library exactly once for the process and registers a clean shutdown at process exit. You can observe the lifecycle:\n\nLibPARI.is_initialized()   # true\nLibPARI.library_state()    # LibraryState.INITIALIZED\nLibPARI.stack_size()       # PARI main-stack size, in bytes\n\nThe PARI main-stack size defaults to 8 MiB. To change it, set the LIBPARI_STACK_SIZE environment variable (a byte count) before the package is loaded.","title":"Loading the package"},{"category":"section","location":"getting-started/#A-first-tour","page":"Getting started","text":"using LibPARI brings in exactly four names: pari, Gen, gp_eval and PariError. Everything else stays qualified.\n\npari converts a Julia value into a PARI one — a Gen — and the usual Julia operators work on it:\n\nusing LibPARI\na = pari(42)\na * a + 1\npari(2)^100\n\npari accepts integers of any magnitude, rationals, floats — including BigFloat, exactly — and complex numbers. Mixed arithmetic takes a Julia operand on either side:\n\npari(3 // 4) + 1\npari(2)^200 == big(2)^200\npari(big\"1.234567890123456789012345678901\")\n\nConvert back to a Julia value:\n\nBigInt(pari(2)^64)\nInt(pari(255))","title":"A first tour"},{"category":"section","location":"getting-started/#The-three-levels-of-access","page":"Getting started","text":"1. Idiomatic Julia. A small hand-written surface: the operators above, the conversions, and a short list of Base functions where PARI's operation is Julia's.\n\ngcd(pari(12), 18)\nfactorial(pari(20))\nLibPARI.isprime(1009)\nLibPARI.factors(60)\n\nThis layer is the one promised stable at 1.0.\n\n2. The generated bindings. Nearly 1200 PARI functions under LibPARI.PARI, one per eligible entry in PARI's own function database, each carrying PARI's help text as its docstring:\n\nLibPARI.PARI.nextprime(pari(1000))\nLibPARI.PARI.eulerphi(pari(100))\n\nTheir names and calling conventions follow pari.desc, so they track PARI rather than Julia taste.\n\n3. GP expressions. gp_eval reaches anything the other two do not — in particular the functions taking a GP closure, which are not generated:\n\ngp_eval(\"sum(k = 1, 100, k^2)\")\n\nIt is an escape hatch, not the recommended way to use LibPARI.","title":"The three levels of access"},{"category":"section","location":"getting-started/#Handling-errors","page":"Getting started","text":"A failing PARI call raises a catchable PariError carrying PARI's own message and an error category:\n\ntry\n    gp_eval(\"1/0\")\ncatch e\n    e isa PariError\nend","title":"Handling errors"},{"category":"section","location":"getting-started/#Next-steps","page":"Getting started","text":"The API reference documents the hand-written public surface in full and explains how the generated LibPARI.PARI bindings are organized.","title":"Next steps"},{"category":"section","location":"precision/#Precision","page":"Precision","text":"PARI computes with reals to a working precision. LibPARI states that precision in bits everywhere on its own surface, and converts only at the ccall boundary.","title":"Precision"},{"category":"section","location":"precision/#The-units","page":"Precision","text":"PARI's own units are easy to misread, so they are pinned here against the headers shipped with PARI_jll (PARI 2.17):\n\nA prototype p or b argument is a bit count. pariinl.h defines prec2nbits(long x) { return x; } — the value is already in bits.\nPARI allocates a t_REAL to a whole number of machine words, so a request is rounded up: nbits2prec(x) = ceil(x / 64) * 64 on a 64-bit platform.\n\nLibPARI.nbits2prec exposes that rounding:\n\nrequested bits allocated bits\n1 64\n4 64\n64 64\n65 128\n128 128\n200 256\n\nnote: A historical mistake, now fixed\nBefore version 0.18.0 every generated binding defaulted to prec = 4, with a comment describing it as \"word precision\". It was in fact a request for 4 bits, which PARI rounded up to its 64-bit minimum — not a deliberate choice. Bindings now default to LibPARI's working precision.","title":"The units"},{"category":"section","location":"precision/#The-default","page":"Precision","text":"LibPARI.default_precision is 128 bits, which is GP's own realbitprecision default (about 38 decimal digits — the width PARI prints by default). Matching GP means the same expression gives the same answer in gp and in LibPARI.\n\nTracking Julia's precision(BigFloat) (256 bits) was the alternative. It was rejected: it would make LibPARI disagree with the library it wraps.","title":"The default"},{"category":"section","location":"precision/#Setting-it","page":"Precision","text":"setprecision(LibPARI.Gen, 256) do\n    LibPARI.PARI.mppi()        # computed to 256 bits\nend\n\nScopes nest and unwind, including when the body throws:\n\nsetprecision(LibPARI.Gen, 256) do\n    setprecision(LibPARI.Gen, 512) do\n        precision(LibPARI.Gen)   # 512\n    end\n    precision(LibPARI.Gen)       # back to 256\nend\n\nprecision(LibPARI.Gen) reads the value in force; precision(g::LibPARI.Gen) reads the accuracy of a real value, and raises ArgumentError for an exact one — test with LibPARI.isexact.","title":"Setting it"},{"category":"section","location":"precision/#What-the-scope-reaches,-and-what-it-does-not","page":"Precision","text":" follows setprecision(Gen, …)\ngenerated bindings taking p or b yes — it is their keyword default\nGen^Gen and the hand-written numeric API yes\nconversions into and out of a Gen value-preserving, so unaffected\ngp_eval no — see below\na task spawned inside the scope no — see below\n\ngp_eval follows PARI's process-global realprecision, not the scope. The GP interpreter reads its own global default, which is shared by every thread in the process; LibPARI does not write it behind your back. Set it from GP itself if you need to:\n\nLibPARI.gp_eval(\"default(realbitprecision, 512)\")\n\nThe scope is task-local. It nests and unwinds correctly, and it does cross the hop onto LibPARI's PARI worker tasks — the precision is read in the calling task and carried into the closure that gets marshalled. But a task you spawn inside the scope does not inherit it:\n\nsetprecision(LibPARI.Gen, 512) do\n    fetch(Threads.@spawn precision(LibPARI.Gen))   # the default, not 512\nend\n\nThis is a limitation of the Julia 1.10 LTS floor, which has no Base.ScopedValues; a ScopedValue would be inherited. Set the precision inside the spawned task, or revisit when the compat floor moves to 1.11.","title":"What the scope reaches, and what it does not"},{"category":"section","location":"precision/#Precision-and-conversion","page":"Precision","text":"Gen(x) preserves x exactly, whatever its width:\n\nx = big\"1.00000000000000000000000000000001\"\nBigFloat(LibPARI.Gen(x)) == x        # true\n\nA wide float is decomposed into its exact integer significand and binary exponent and rebuilt at a precision that holds every bit — nothing passes through Cdouble. An IEEEFloat takes PARI's dbltor, which is exact.\n\nComing back, BigFloat(g) is exact by default: the result carries PARI's own mantissa, at whatever precision that takes, which may exceed the ambient precision(BigFloat). Pass the keyword to round once to a chosen width instead:\n\nBigFloat(g; precision = 256)\n\nInf, -Inf and NaN raise InexactError: PARI's t_INFINITY exists but does not take part in general arithmetic, so mapping onto it would produce values that fail later, far from the conversion.","title":"Precision and conversion"},{"category":"section","location":"precision/#Series-precision","page":"Precision","text":"A series precision (prototype code P) is a number of terms, not a bit count. It has its own default and is deliberately outside the precision scope.","title":"Series precision"},{"category":"section","location":"precision/#Reference","page":"Precision","text":"","title":"Reference"},{"category":"method","location":"precision/#LibPARI.Gen-Tuple{AbstractFloat}","page":"Precision","text":"Gen(x::AbstractFloat) -> Gen\n\n\nConvert a Julia floating-point number to a Gen (a PARI t_REAL), preserving the value exactly.\n\nAn IEEEFloat (Float16, Float32, Float64) goes through PARI's dbltor, which is exact. Any wider float — a BigFloat, or a third-party AbstractFloat — is decomposed into its exact integer significand and binary exponent and rebuilt at a precision that holds every bit, so nothing passes through Cdouble (REQ-PREC-08).\n\nInf, -Inf and NaN raise InexactError: PARI's t_INFINITY does not take part in general arithmetic (REQ-PROM-10).\n\nExamples\n\njulia> using LibPARI\n\njulia> BigFloat(LibPARI.Gen(big\"1.00000000000000000000000000000001\")) ==\n           big\"1.00000000000000000000000000000001\"\ntrue\n\n\n\n\n\n","title":"LibPARI.Gen"},{"category":"method","location":"precision/#Base.MPFR.setprecision-Tuple{Function, Type{Gen}, Integer}","page":"Precision","text":"setprecision(\n    f::Function,\n    _::Type{Gen},\n    bits::Integer\n) -> Any\n\n\nRun f with LibPARI's working precision set to bits, then restore it.\n\nScopes nest, and unwind even when f throws. The setting is task-local: the calls f makes are marshalled onto LibPARI's PARI worker tasks, and the precision is read in the calling task and carried across that boundary — but a task spawned inside the scope does not inherit it (Julia 1.10 has no ScopedValues).\n\nThe scope governs the calls LibPARI itself makes. It does not reach into the GP interpreter: gp_eval follows PARI's process-global realprecision.\n\nExamples\n\njulia> using LibPARI\n\njulia> setprecision(LibPARI.Gen, 256) do\n           precision(LibPARI.Gen)\n       end\n256\n\n\n\n\n\n","title":"Base.MPFR.setprecision"},{"category":"method","location":"precision/#Base.precision-Tuple{Gen}","page":"Precision","text":"precision(g::Gen) -> Int64\n\n\nThe accuracy of a real Gen, in bits.\n\nPARI stores a t_REAL to a whole number of machine words, so the answer is a multiple of the word size. An exact Gen has no finite precision and raises ArgumentError rather than returning an invented number — test with isexact first.\n\nExamples\n\njulia> using LibPARI\n\njulia> precision(LibPARI.Gen(1.5)) >= 64\ntrue\n\n\n\n\n\n","title":"Base.precision"},{"category":"method","location":"precision/#Base.precision-Tuple{Type{Gen}}","page":"Precision","text":"precision(_::Type{Gen}) -> Int64\n\n\nThe working precision in force for the current task, in bits.\n\nEqual to default_precision outside any setprecision(Gen, bits) scope.\n\nExamples\n\njulia> using LibPARI\n\njulia> precision(LibPARI.Gen) == LibPARI.default_precision()\ntrue\n\n\n\n\n\n","title":"Base.precision"},{"category":"method","location":"precision/#LibPARI.default_precision-Tuple{}","page":"Precision","text":"default_precision() -> Int64\n\n\nThe working precision LibPARI supplies to PARI, in bits, when a caller does not give one.\n\nDefaults to 128 bits — GP's own realbitprecision default — so that an expression evaluates to the same thing in gp and in LibPARI. Change it for a dynamic extent with setprecision(Gen, bits).\n\nExamples\n\njulia> using LibPARI\n\njulia> LibPARI.default_precision()\n128\n\n\n\n\n\n","title":"LibPARI.default_precision"},{"category":"method","location":"precision/#LibPARI.isexact-Tuple{Gen}","page":"Precision","text":"isexact(g::Gen) -> Bool\n\n\nWhether g is an exact PARI value — an integer, a rational, or any other type PARI stores without a working precision.\n\nExamples\n\njulia> using LibPARI\n\njulia> LibPARI.isexact(LibPARI.Gen(42))\ntrue\n\n\n\n\n\n","title":"LibPARI.isexact"},{"category":"method","location":"precision/#LibPARI.nbits2prec-Tuple{Integer}","page":"Precision","text":"nbits2prec(bits::Integer) -> Int64\n\n\nRound a request of bits up to the precision PARI will actually allocate — a whole number of machine words.\n\nThis mirrors PARI's own nbits2prec.\n\nExamples\n\njulia> using LibPARI\n\njulia> LibPARI.nbits2prec(65)\n128\n\n\n\n\n\n","title":"LibPARI.nbits2prec"},{"category":"method","location":"precision/#LibPARI.set_global_precision!-Tuple{Integer}","page":"Precision","text":"set_global_precision!(bits::Integer) -> Int64\n\n\nSet PARI's process-global working precision, in bits — the default the GP interpreter reads.\n\nThis is the setting gp_eval follows; the setprecision(Gen, bits) scope deliberately does not touch it. Unlike that scope it is global and shared by every thread and task in the process, so prefer the scope unless you specifically need to change what GP itself does.\n\nReturns the requested bits.\n\nThis replaces the 47 zero-argument sd_* bindings the generator used to emit for PARI's Class: default records. Those called C functions declared (const char *, long) with no arguments set, which is undefined behaviour; they are no longer generated (REQ-PREC-13).\n\nExamples\n\njulia> using LibPARI\n\njulia> old = LibPARI.set_global_precision!(256);\n\njulia> precision(LibPARI.gp_eval(\"Pi\")) >= 256\ntrue\n\njulia> LibPARI.set_global_precision!(old);  # always restore: it is global\n\n\n\n\n\n\n","title":"LibPARI.set_global_precision!"},{"category":"section","location":"mcp/#MCP-connector","page":"MCP connector","text":"LibPARI ships an optional MCP (Model Context Protocol) connector — a Julia package extension that exposes PARI/GP to AI assistants and agents. An MCP client can then run exact number theory through PARI — factorisation, primality testing, modular arithmetic, elliptic curves — instead of approximating or hallucinating it.","title":"MCP connector"},{"category":"section","location":"mcp/#Optional-by-design","page":"MCP connector","text":"The connector is a package extension. Its code, and its dependency ModelContextProtocol, exist in your process only if you deliberately load the MCP library. A project that depends on LibPARI alone installs no MCP stack, loads no connector code, and behaves exactly as without this feature. Installing LibPARI never installs ModelContextProtocol.","title":"Optional by design"},{"category":"section","location":"mcp/#Setup","page":"MCP connector","text":"Install the MCP server library once:\n\nusing Pkg\nPkg.add(\"ModelContextProtocol\")","title":"Setup"},{"category":"section","location":"mcp/#Running-the-server","page":"MCP connector","text":"Loading ModelContextProtocol alongside LibPARI activates the connector. A one-line launcher script is the MCP server:\n\n# libpari_mcp.jl\nusing LibPARI\nusing ModelContextProtocol      # activates the connector\nLibPARI.serve_mcp()             # serve over stdio (blocking)\n\nConfigure any MCP client (for example Claude Desktop) to run it as a server:\n\njulia --project=. libpari_mcp.jl","title":"Running the server"},{"category":"section","location":"mcp/#The-tools","page":"MCP connector","text":"The connector advertises three MCP tools:\n\nTool Purpose Example arguments\npari_eval Evaluate a GP-language expression — reaches every PARI function {\"expression\": \"factor(2^67-1)\"}\npari_functions List or search PARI's functions {\"query\": \"prime\"}\npari_help Return a PARI function's help text {\"name\": \"nextprime\"}\n\nBecause a GP expression can call any PARI function, pari_eval alone already exposes the whole of PARI; pari_functions and pari_help let an agent discover what to put in an expression.\n\nEvery tool is robust: a malformed expression, an unknown function, or a PARI error (division by zero, a domain error) is returned to the client as a structured error, and the server keeps running.","title":"The tools"},{"category":"section","location":"mcp/#Example-prompts","page":"MCP connector","text":"Once the connector is running and your MCP client is connected, just ask your AI assistant in plain English — it calls the PARI tools and reports the exact answer.\n\ntip: Name LibPARI in your prompt\nIf your MCP client also has other computer-algebra servers connected (for example a Giac/Xcas server), name LibPARI explicitly so the assistant routes the request to this server. Say \"LibPARI\" (the server is registered as libpari), or name a tool directly — pari_eval, pari_functions, or pari_help. The prompts below all do this.\n\nTry prompts like these, from simple to advanced:\n\nBasic arithmetic\n\n\"Using LibPARI, what is 2 to the power of 100?\"\n\"With LibPARI, compute 100 factorial.\"\n\"Ask LibPARI for the greatest common divisor of 1071 and 462.\"\n\nPrimes\n\n\"Using LibPARI, is 2^127 - 1 a prime number?\"\n\"With LibPARI, what is the next prime after one billion?\"\n\"Using LibPARI, how many prime numbers are there below one million?\"\n\nFactorisation & number theory\n\n\"Using LibPARI, give me the prime factorisation of 2^67 - 1.\"\n\"With LibPARI, what is Euler's totient of 1000000?\"\n\"Ask LibPARI for the sum of the divisors of 360.\"\n\"Using LibPARI, how many divisors does 720 have?\"\n\nPolynomials & symbolic\n\n\"Using LibPARI, factor the polynomial x^4 - 1.\"\n\"With LibPARI, find the roots of x^3 - 2.\"\n\nModular & advanced\n\n\"Using LibPARI, compute 2 raised to the power 1000000, modulo 1000000007.\"\n\"With LibPARI, what is the trace of Frobenius of the elliptic curve y^2 = x^3 + 1 at the prime 101?\"\n\nExploring PARI\n\n\"Using LibPARI, which PARI functions deal with elliptic curves?\"\n\"With LibPARI, show me the help for the nextprime function.\"\n\"Use LibPARI's pari_functions tool to list what is available for primality testing.\"\n\nThe assistant turns each request into a pari_eval, pari_functions, or pari_help call and answers with PARI's exact result. The next section shows the underlying tool calls.","title":"Example prompts"},{"category":"section","location":"mcp/#Quick-examples","page":"MCP connector","text":"A tour of PARI through the connector, from simple to advanced — every result below is exact. Each row is a pari_eval call: the expression argument and the result the tool returns.","title":"Quick examples"},{"category":"section","location":"mcp/#Start-here-—-basic-arithmetic","page":"MCP connector","text":"expression Result\n2 + 2 4\n2^100 1267650600228229401496703205376\n10! 3628800\n1/3 + 1/6 1/2 (exact rationals)\ngcd(1071, 462) 21","title":"Start here — basic arithmetic"},{"category":"section","location":"mcp/#Primes","page":"MCP connector","text":"expression Result\nisprime(97) 1 (true)\nnextprime(1000) 1009\nprimepi(1000) 168 (number of primes ≤ 1000)\nisprime(2^127 - 1) 1 (the Mersenne prime M₁₂₇)","title":"Primes"},{"category":"section","location":"mcp/#Factorisation-and-number-theory","page":"MCP connector","text":"expression Result\nfactor(360) [2 3; 3 2; 5 1] — rows are [prime, exponent]: 2³·3²·5\neulerphi(100) 40 (Euler's totient)\nsigma(12) 28 (sum of divisors)\nbinomial(20, 10) 184756\nfactor(2^67 - 1) [193707721 1; 761838257287 1] (Cole's 1903 factorisation)","title":"Factorisation & number theory"},{"category":"section","location":"mcp/#Modular,-symbolic-and-numeric","page":"MCP connector","text":"expression Result\nsum(k=1, 100, k^2) 338350 (a GP-closure function)\nlift(Mod(2, 1000000007)^1000000) 235042059 (modular exponentiation)\nfactor(x^4 - 1) factors x - 1, x + 1, x^2 + 1\npolroots(x^3 - 2) ≈ 1.259921 (the real ∛2) and two complex conjugate roots\nellap(ellinit([0,1]), 101) 0 (trace of Frobenius — 0 ⇒ supersingular at 101)","title":"Modular, symbolic & numeric"},{"category":"section","location":"mcp/#Discovery-—-pari_functions-and-pari_help","page":"MCP connector","text":"Tool Arguments Result\npari_functions {\"query\": \"ellis\"} elliscm, ellisdivisible, ellisogeny, ellisoncurve, …\npari_help {\"name\": \"nextprime\"} nextprime(x): smallest pseudoprime >= x.\n\nnote: Note\npari_eval speaks the full GP language, so it reaches every PARI function (factor, sum, …). pari_functions and pari_help introspect the generated binding layer, whose names are PARI's C-level names — a few GP names (such as factor) are not binding names there, even though they work inside a pari_eval expression.","title":"Discovery — pari_functions and pari_help"},{"category":"section","location":"mcp/#Entry-point","page":"MCP connector","text":"","title":"Entry point"},{"category":"function","location":"mcp/#LibPARI.serve_mcp","page":"MCP connector","text":"serve_mcp(; start::Bool = true, name = \"libpari\", version = \"0.12.0\")\n\nStart an MCP (Model Context Protocol) server that exposes PARI/GP to MCP clients — AI assistants and agents.\n\nserve_mcp is provided by an optional package extension. It has a callable method only once ModelContextProtocol is loaded alongside LibPARI:\n\nusing LibPARI\nusing ModelContextProtocol   # activates the connector\nLibPARI.serve_mcp()          # serve over stdio (blocking)\n\nThe server exposes three tools: pari_eval (evaluate a GP expression — reaches every PARI function), pari_functions (list and search PARI functions), and pari_help (a function's help text). With start = false the configured server is returned without being started.\n\nCalling serve_mcp without ModelContextProtocol loaded raises a MethodError whose hint tells you to load it. Installing LibPARI does not install ModelContextProtocol — the connector is strictly optional.\n\nExamples\n\njulia> using LibPARI\n\njulia> LibPARI.serve_mcp isa Function\ntrue\n\n\n\n\n\n","title":"LibPARI.serve_mcp"},{"category":"section","location":"api-redesign/#API-redesign-—-the-road-to-1.0.0","page":"API redesign","text":"M0–M10 built a complete PARI/GP wrapper. The redesign described here makes it an honest one: it fixes the public contracts that cannot be changed once LibPARI reaches 1.0.\n\nThis page is the narrative front door to Part II of the roadmap (ROADMAP.md, milestones M11–M21). The roadmap remains the plan of record — deliverable lists, requirement IDs (REQ-TYPE-*, REQ-PROM-*, …) and exit criteria live there. This page states the problem, the proposed contracts, the consolidated breaking changes, and what is still undecided.\n\nwarning: Describes an API that is still being built\nEvery code block below is illustrative, not executable. The \"after\" snippets describe the surface planned for 0.16.0 … 1.0.0; none of it is available in 0.15.1, and the items marked M11 have since landed on the development branch but are not released. Nothing on this page is run at documentation build time.\n\nTwo commitments frame the whole redesign:\n\nCorrectness outranks backward compatibility while the package is pre-1.0. A deprecation shim is added only where it is cheap and unambiguous. A shim that would preserve semantics a milestone classifies as incorrect is rejected, not written.\nThe two-layer architecture is kept. Nothing here rewrites PARI, replaces the generated layer with hand-maintained wrappers, or edits src/bindings.jl by hand.\n\nThe observations in section 1 describe the API as of 0.15.1, before Part II began, and their file:line references are to that tag. The M11 items have since landed on the development branch and are no longer true of that branch — Gen is a PariObject, the mixed operators are explicit, a t_REAL converts through PARI's mantissa, and hash is total. Each observation is pinned by a test before its milestone closes.\n\n","title":"API redesign — the road to 1.0.0"},{"category":"section","location":"api-redesign/#1.-The-problems-with-the-current-API","page":"API redesign","text":"","title":"1. The problems with the current API"},{"category":"section","location":"api-redesign/#1.1-Gen-claims-to-be-a-Julia-Number,-but-wraps-everything","page":"API redesign","text":"mutable struct Gen <: Number (src/gen.jl:46) is a single concrete type for every PARI object — the type tag is a runtime word read from the GEN header (src/gen.jl:167). Matrices, strings and closures therefore all claim to be Julia numbers:\n\ngp_eval(\"[1,2;3,4]\") isa Number   # true — a matrix\ngp_eval(\"\\\"abc\\\"\")   isa Number   # true — a string\n\nThe Number supertype is also load-bearing in ways that are easy to miss: mixed arithmetic (Gen(6) + 1), broadcasting as a scalar, float, abs2, adjoint and several constructors exist only because Base's Number fallbacks apply.\n\nThe ordering claim is already overstated. Only isless(::Gen, ::Gen) is defined (src/numeric.jl:203), so:\n\nsort(Any[Gen(2), 1])   # MethodError today","title":"1.1 Gen claims to be a Julia Number, but wraps everything"},{"category":"section","location":"api-redesign/#1.2-Promotion-is-a-catch-all,-and-it-recurses","page":"API redesign","text":"Base.promote_rule(::Type{Gen}, ::Type{<:Number}) = Gen  # src/numeric.jl:143\nBase.convert(::Type{Gen}, x::Number) = Gen(x)           # src/numeric.jl:145\n\nThese two lines promise support for every Julia Number. The promise cannot be kept, and failing to keep it is not graceful:\n\nGen(1) + π                     # StackOverflowError\npromote_type(Gen, typeof(π))   # StackOverflowError\nGen(MyFloat(1))                # MethodError: no method matching\n                               # Float64(::MyFloat) — raised inside Gen\n\nBecause Gen <: Number, Base also donates constructors nobody designed:\n\nGen('a') == 97                      # inherited, and lossy\nconvert(Gen, CartesianIndex(3))     # == 3\nGen(Base.TwicePrecision(1.0, 0.0))  # silently narrows\n\nconvert(::Type{Gen}, x::Integer) (src/conversions.jl:120) is a second, overlapping conversion path.","title":"1.2 Promotion is a catch-all, and it recurses"},{"category":"section","location":"api-redesign/#1.3-Real-conversions-go-through-decimal-text","page":"API redesign","text":"Float64(::Gen) and BigFloat(::Gen) re-parse PARI's printed decimal representation (src/conversions.jl:213 and src/conversions.jl:235, entry point src/conversions.jl:228):\n\nFloat64(Gen(1e-10))\n# ArgumentError: cannot parse \"1.0000000000000000364 E-10\"\n\nFloat64(gp_eval(\"1.0*10^400\"))\n# ArgumentError — PARI prints a space before the exponent\n\nAny magnitude outside roughly [1e-5, 1e19) is affected. This violates REQ-CONV-04, and it makes hash partial: hash(::Gen) routes a T_REAL through Float64 (src/numeric.jl:217), so hashing a large real throws. It is also lossy in the other direction — two 1024-bit reals that differ beyond 53 bits hash equal while gequal may call them distinct.\n\nTyped outward conversions are missing outright:\n\nFloat32(Gen(1))                # MethodError\nFloat16(Gen(1))                # MethodError\nRational{Int}(Gen(1)/Gen(2))   # MethodError\nComplex{BigFloat}(Gen(1))      # MethodError\n\nwhile (::Type{T})(g::Gen) where {T<:Integer} (src/conversions.jl:181) is far too wide.","title":"1.3 Real conversions go through decimal text"},{"category":"section","location":"api-redesign/#1.4-Precision-is-undocumented,-mis-labelled-and-lossy","page":"API redesign","text":"const _DEFAULT_PREC = Int(4) (src/numeric.jl:8) is an unexplained magic number, read by Gen^Gen (src/numeric.jl:80).\nThe generator's comment DEFAULT_PREC = 4 # word precision (prototype code p) (gen/generate.jl:19) is wrong for PARI ≥ 2.15, where the prototype codes p and b are bit counts. 142 generated bindings carry prec::Integer = 4 as a result.\nGen(x::AbstractFloat) calls dbltor(Cdouble(x)) (src/numeric.jl:111), so a BigFloat collapses to 53 bits with no diagnostic.\nThere is no precision(::Gen), no way to set a working precision, and no documented unit anywhere — docs/src/api.md:28 says \"pass a prec keyword\" without naming what prec counts.\nThe generator emits Class: default records whose empty Prototype: produces 0-argument bindings for C functions declared (const char *, long). Calling LibPARI.PARI.sd_realprecision() today reads two argument registers the binding never sets.","title":"1.4 Precision is undocumented, mis-labelled and lossy"},{"category":"section","location":"api-redesign/#1.5-The-public-surface-is-unfrozen,-and-conversion-has-no-front-door","page":"API redesign","text":"names(LibPARI) is [:LibPARI] — nothing is exported, so every use site is fully qualified, and there is no single documented way to turn a Julia value into a PARI one. Complex construction goes through the GP parser on every call:\n\nGen(z::Complex) = Gen(real(z)) + Gen(imag(z)) * gp_eval(\"I\")\n# src/numeric.jl:179\n\nThis is correct (GP refuses I = 5, so I cannot be shadowed), but it ties a core constructor to the evaluator and runs a parser per value.","title":"1.5 The public surface is unfrozen, and conversion has no front door"},{"category":"section","location":"api-redesign/#1.6-Generated-bindings-accept-only-Gen","page":"API redesign","text":"LibPARI.PARI.nextprime(1000)    # MethodError\n\nEvery G slot in the 1241 generated bindings is typed ::LibPARI.Gen, and the ~189 optional D<G> keywords are untyped:\n\nfunction alggroupcenter(x1::LibPARI.Gen; x2 = nothing)  # src/bindings.jl:6042\n\nso an unsupported keyword value fails late, with type Int64 has no field ptr. Two further defects sit in the same generated code: ~86 G-taking bindings on the scalar/void return branch never capture and restore avma (protected_call restores it only on error), and Gen arguments are rooted across the ccall only implicitly, through closure captures, with no GC.@preserve region.","title":"1.6 Generated bindings accept only Gen"},{"category":"section","location":"api-redesign/#1.7-gp_eval-has-hidden,-undocumented-persistent-state","page":"API redesign","text":"gp_eval(s::AbstractString) (src/evaluator.jl:27) mutates a GP environment that is never named in the API. What that environment is under the per-thread PARI contexts (src/concurrency.jl:93 onward, pari_thread_alloc) is currently unverified: whether the variable table is process-global or per-context, and whether a variable assigned on one worker is visible from another, are open questions rather than documented facts.","title":"1.7 gp_eval has hidden, undocumented persistent state"},{"category":"section","location":"api-redesign/#1.8-PARI's-container-types-have-no-Julia-interface","page":"API redesign","text":"A t_VEC, t_COL, t_VECSMALL or t_MAT Gen supports no length, size, getindex or iterate. Reaching an element means going back through gp_eval or a generated binding.","title":"1.8 PARI's container types have no Julia interface"},{"category":"section","location":"api-redesign/#1.9-show-and-print-are-the-same-call","page":"API redesign","text":"Base.show(io::IO, g::Gen)  = print(io, _genrepr(g))  # src/conversions.jl:252\nBase.print(io::IO, g::Gen) = print(io, _genrepr(g))  # src/conversions.jl:254\n\nso repr(g) is bare PARI text, and a Gen wrapping a t_STR is indistinguishable in repr from a Julia String. Every rendering is also real PARI work (GENtostr under protected_call) plus a hop onto the PARI worker, with no truncation policy for very large values.\n\n","title":"1.9 show and print are the same call"},{"category":"section","location":"api-redesign/#2.-The-proposed-contracts-at-1.0","page":"API redesign","text":"LibPARI offers three levels of access. They carry different stability promises, and the difference is deliberate.","title":"2. The proposed contracts at 1.0"},{"category":"section","location":"api-redesign/#Layer-1-—-the-idiomatic-Julia-core-(**promised-stable**)","page":"API redesign","text":"This is the only layer covered by LibPARI's SemVer promise at 1.0.\n\nType contract. abstract type PariObject end; Gen <: PariObject. Gen stays concrete, mutable, single-field and unparameterised — a parametric Gen{tag} is rejected, because the tag is a runtime word and a parametric return type would make every generated binding uninferable. Gen is not a Julia Number, and LibPARI never claims a Gen is a drop-in for one.\n\nConversion contract. One documented accepted-input set, LibPARI.PariConvertible, and one conversion entry point, LibPARI.gen_convert(x)::Gen. Gen(x), convert(Gen, x), pari(x) and every mixed operator route through it; no second path exists. Anything outside the set raises LibPARI.ConversionError at the boundary, naming the offending type. Guaranteed inward: Bool, Int8…Int128, UInt8…UInt128, BigInt, Rational{<:Integer}, Float16, Float32, Float64, BigFloat, and Complex{T} over those. Guaranteed outward: BigInt, Bool, Base.BitInteger, Float16/Float32/Float64, BigFloat, Rational{T}, Complex{T} — each raising InexactError rather than a parse failure.\n\nOperator contract. + - * / ^ \\ == < <= isless exist as explicit two-argument methods on (Gen, PariConvertible) and (PariConvertible, Gen), generated from one operator table, each calling gen_convert exactly once. Promotion plays no role and promote_rule is removed.\n\nPrecision contract. Precision is counted in bits, everywhere. Base.precision(g::Gen)::Int, LibPARI.isexact(g), LibPARI.default_precision(), and a caller-side scope setprecision(f, Gen, bits) / setprecision(Gen, bits) that nests and restores. The scope is read in the caller task and captured into the closure marshalled onto the PARI worker; it never writes PARI's global precreal.\n\nFacade. A deliberately small set of Base extensions where the semantics match exactly (gcd, gcdx, numerator, denominator, factorial, powermod, invmod, a sign-corrected mod), plus an unexported number-theory and polynomial facade (isprime, nextprime, prevprime, factor, factors, Mod, lift, degree, coeff, subst, polroots). These exist because they add semantics — a Julia Int return, a domain check, Julia's sign convention — never to rename a generated binding.\n\nContainers. length, size, axes, getindex, iterate, eltype, collect, Vector{Gen} and Matrix{Gen} for T_VEC, T_COL, T_VECSMALL and T_MAT, one-based, with BoundsError out of range and a typed error naming the PARI type for a non-container Gen. Gen does not subtype AbstractArray: a runtime-tagged value cannot honestly promise a static array interface. Element access clones — documented and tested.\n\nDisplay. show(io, g) is compact and self-identifying; show(io, MIME\"text/plain\"(), g) keeps PARI notation for the REPL; print(io, g) stays PARI's own text, so string interpolation is unaffected.\n\nExported surface. Exactly four names — Gen, pari, gp_eval, PariError — locked by a test that fails CI if names(LibPARI) changes. Supported-but-unexported names (PARI, PariType, PariErr, gentype, is_initialized, library_state, stack_size, serve_mcp) are marked public behind a VERSION >= v\"1.11\" guard.\n\nUnchanged invariants. Every result is cloned off PARI's transient stack and avma is restored; PARI errors stay catchable PariErrors; libpari calls stay marshalled onto the per-thread workers; the public surface stays @inferred-clean; no exported or public signature mentions Ptr.","title":"Layer 1 — the idiomatic Julia core (promised stable)"},{"category":"section","location":"api-redesign/#Layer-2-—-the-generated-LibPARI.PARI-bindings-(**shape-stable**)","page":"API redesign","text":"One method per eligible PARI function, keeping PARI's own name and docstring. Each binding returns a Gen (or the scalar Julia type its prototype calls for) and routes through the same safe, leak-free, thread-safe boundary as the core.\n\nThe redesign widens this layer's inputs: a G slot accepts PariConvertible, an optional D<G> keyword becomes Union{Nothing,PariConvertible} = nothing, and a p/b precision keyword defaults to LibPARI.default_precision() in bits. Exactly one conversion happens per argument; an existing Gen is never copied; every binding still has exactly one method. Parameters stay named x1, x2, … because pari.desc carries no argument-name field — a limitation documented, not papered over with invented names.\n\nStability promise: the shape and the calling conventions of this layer are stable at 1.0. Individual signatures are not: they are derived from PARI's pari.desc, so a PARI upgrade may add, remove or retype bindings without a LibPARI major bump. Code that must be immune to that should go through Layer 1.","title":"Layer 2 — the generated LibPARI.PARI bindings (shape stable)"},{"category":"section","location":"api-redesign/#Layer-3-—-gp_eval-and-GP-sessions-(**escape-hatch**)","page":"API redesign","text":"gp_eval(str) keeps its signature and becomes documented as the default session; GPSession and gp_eval(session, str) make the persistent state explicit, with reset!(session) clearing an explicitly documented set. If truly independent GP environments turn out not to be reachable through libpari as embedded here, LibPARI will say so and ship the safest honest abstraction instead of pretending to isolation it cannot provide.\n\ngp_eval stays documented as the escape hatch — in particular for the PARI functions taking GP closure arguments (E/I/V/C), which have no other route — and never as the primary API. It follows PARI's process-global precreal, not the setprecision(Gen, …) scope, and the documentation states this explicitly.\n\n","title":"Layer 3 — gp_eval and GP sessions (escape hatch)"},{"category":"section","location":"api-redesign/#3.-Breaking-changes","page":"API redesign","text":"Consolidated across M11–M18. \"Shim\" records whether a deprecation path ships.","title":"3. Breaking changes"},{"category":"section","location":"api-redesign/#3.1-Gen-is-no-longer-a-Julia-Number-(M11)","page":"API redesign","text":"No shim is possible — a Julia type's supertype is fixed at definition. Code dispatching on ::Number — LinearAlgebra methods bounded by T<:Number, generic numeric packages, x isa Number guards — stops accepting Gen.\n\n# before\nGen(2) isa Number                 # true\ngp_eval(\"[1,2;3,4]\") isa Number   # true  (a matrix!)\n\n# after\nGen(2) isa Number                 # false\nGen(2) isa LibPARI.PariObject     # true\ngp_eval(\"[1,2;3,4]\") isa Number   # false","title":"3.1 Gen is no longer a Julia Number (M11)"},{"category":"section","location":"api-redesign/#3.2-Mixed-arithmetic-and-comparison-become-explicit-methods-(M11,-M12)","page":"API redesign","text":"Behaviour is preserved for the documented type set; the mechanism changes from promotion to explicit two-argument methods.\n\n# before — resolved by Base's op(::Number, ::Number) + promote_rule\nGen(6) + 1\nGen(2) == 2\nDict(Gen(7) => \"s\")[7]\n\n# after — resolved by +(::Gen, ::PariConvertible) etc.; same results\nGen(6) + 1\nGen(2) == 2\nDict(Gen(7) => \"s\")[7]\n\nOnly the outcome for unsupported types changes:\n\n# before\nGen(1) + π                    # StackOverflowError\npromote_type(Gen, typeof(π))  # StackOverflowError\n\n# after\nGen(1) + π                    # LibPARI.ConversionError: Irrational{:π}\n                              # is not PariConvertible (see gen_convert)","title":"3.2 Mixed arithmetic and comparison become explicit methods (M11, M12)"},{"category":"section","location":"api-redesign/#3.3-promote_rule-and-the-convert-catch-all-are-removed-(M12)","page":"API redesign","text":"No shim: restoring the catch-all restores the failures it caused.\n\n# before\nBase.promote_rule(::Type{Gen}, ::Type{<:Number}) = Gen\nBase.convert(::Type{Gen}, x::Number) = Gen(x)\nconvert(Gen, 2.5) isa Gen     # via the catch-all\n\n# after — no promote_rule at all\nconvert(Gen, 2.5) isa Gen     # via convert(::Type{Gen}, ::PariConvertible)\nconvert(Gen, MyFloat(1))      # LibPARI.ConversionError","title":"3.3 promote_rule and the convert catch-all are removed (M12)"},{"category":"section","location":"api-redesign/#3.4-Constructors-inherited-from-Number-are-withdrawn-(M12)","page":"API redesign","text":"No shim — blessing an unintended, partly lossy API.\n\n# before\nGen('a') == 97                       # true\nconvert(Gen, CartesianIndex(3)) == 3 # true\nGen(Base.TwicePrecision(1.0, 0.0))   # silently narrows\n\n# after\nGen('a')                             # LibPARI.ConversionError\nconvert(Gen, CartesianIndex(3))      # LibPARI.ConversionError\nGen(Base.TwicePrecision(1.0, 0.0))   # LibPARI.ConversionError","title":"3.4 Constructors inherited from Number are withdrawn (M12)"},{"category":"section","location":"api-redesign/#3.5-T(g::Gen)-narrows-to-BitInteger/BigInt/Bool-(M12)","page":"API redesign","text":"Deprecation-warning shim shipped — an unknown T<:Integer is routed through T(BigInt(g)) behind a warning; semantics are unchanged for well-behaved T.\n\n# before\nMyInt(Gen(5))   # silently resolved by the T<:Integer catch-all\n\n# after\nMyInt(Gen(5))   # same value, with a deprecation warning\nInt(Gen(5))     # unchanged","title":"3.5 T(g::Gen) narrows to BitInteger/BigInt/Bool (M12)"},{"category":"section","location":"api-redesign/#3.6-Real-conversions-stop-parsing-decimal-text-(M11,-M12,-M13)","page":"API redesign","text":"No shim: the old path is a defect.\n\n# before\nFloat64(Gen(1e-10))              # ArgumentError: cannot parse\n                                 # \"1.0000000000000000364 E-10\"\nFloat64(gp_eval(\"1.0*10^400\"))   # ArgumentError\nhash(gp_eval(\"1.0*10^400\"))      # throws, via Float64\n\n# after\nFloat64(Gen(1e-10)) === 1.0e-10  # true\nFloat64(gp_eval(\"1.0*10^400\"))   # a finite Float64, or Inf\nhash(gp_eval(\"1.0*10^400\"))      # an ordinary UInt","title":"3.6 Real conversions stop parsing decimal text (M11, M12, M13)"},{"category":"section","location":"api-redesign/#3.7-BigFloat-no-longer-round-trips-through-53-bits-(M13)","page":"API redesign","text":"No shim — a shim would preserve the silent crush. Opt in to doubles explicitly with pari(Float64(x)).\n\n# before\nx = BigFloat(π; precision = 512)\nBigFloat(Gen(x)) == x            # false — Gen(x) held 53 bits\n\n# after\nBigFloat(Gen(x); precision = 512) == x   # true\nprecision(Gen(x)) >= precision(x)        # true","title":"3.7 BigFloat no longer round-trips through 53 bits (M13)"},{"category":"section","location":"api-redesign/#3.8-Precision-defaults-change-unit-and-value-(M13)","page":"API redesign","text":"No shim: results become more accurate and slightly larger. Callers passing prec= explicitly are unaffected — but the unit they pass is now documented as bits.\n\n# before — 142 bindings, unexplained\nPARI.mppi()               # emitted as prec::Integer = 4 (words? bits?)\n\n# after\nPARI.mppi()               # prec::Integer = LibPARI.default_precision()\nLibPARI.default_precision()          # bits, with documented provenance\nsetprecision(Gen, 1024) do\n    PARI.mppi()                      # 1024-bit π\nend","title":"3.8 Precision defaults change unit and value (M13)"},{"category":"section","location":"api-redesign/#3.9-Zero-argument-Class:-default-bindings-are-replaced-by-stubs-(M13)","page":"API redesign","text":"Throwing shim shipped — the same-named method raises an ArgumentError naming the replacement. Keeping the old call is undefined behaviour: the C function reads two argument registers the binding never set.\n\n# before\nPARI.sd_realprecision()   # undefined behaviour\n\n# after\nPARI.sd_realprecision()   # ArgumentError: use\n                          # LibPARI.set_global_precision!(bits)\nLibPARI.set_global_precision!(256)","title":"3.9 Zero-argument Class: default bindings are replaced by stubs (M13)"},{"category":"section","location":"api-redesign/#3.10-Inf/NaN-acquire-a-stated-policy-(M12)","page":"API redesign","text":"Both old and new behaviours are catchable; only the exception changes.\n\n# before\nGen(Inf)   # PariError(e_OVERFLOW), from dbltor\n\n# after\nGen(Inf)   # the REQ-PROM-10 policy — working assumption: InexactError","title":"3.10 Inf/NaN acquire a stated policy (M12)"},{"category":"section","location":"api-redesign/#3.11-LibPARI-exports-four-names-(M14)","page":"API redesign","text":"No shim can exist for a new export. A script that also uses a package exporting any of Gen, pari, gp_eval or PariError gets an ambiguous-binding error at first use. Mitigation: the list stays at four, and a lock test fails CI if it grows.\n\n# before\nnames(LibPARI)                 # [:LibPARI]\nusing LibPARI\nLibPARI.Gen(2)                 # qualification required\n\n# after\nnames(LibPARI)                 # [:Gen, :LibPARI, :PariError, :gp_eval, :pari]\nusing LibPARI\npari(2)                        # unqualified","title":"3.11 LibPARI exports four names (M14)"},{"category":"section","location":"api-redesign/#3.12-Rational(g)-fails-earlier;-complex-construction-skips-GP-(M14)","page":"API redesign","text":"Same exception type, different message; values identical.\n\n# before\nRational(gp_eval(\"x/2\"))   # InexactError from inside BigInt(PARI.numerator(g))\nGen(3 + 4im)               # runs the GP parser, via gp_eval(\"I\")\n\n# after\nRational(gp_eval(\"x/2\"))   # InexactError from numerator(::Gen), before\n                           # any PARI call\npari(3 + 4im)              # direct PARI construction; gentype === T_COMPLEX","title":"3.12 Rational(g) fails earlier; complex construction skips GP (M14)"},{"category":"section","location":"api-redesign/#3.13-Generated-binding-signatures-widen-(M15)","page":"API redesign","text":"Purely widening — every existing call still compiles. Only code relying on a MethodError breaks.\n\n# before\nPARI.nextprime(x1::LibPARI.Gen)\nPARI.nextprime(1000)                  # MethodError\nPARI.alggroupcenter(x1::Gen; x2 = nothing)   # untyped keyword\n\n# after\nPARI.nextprime(x1::LibPARI.PariConvertible)\nPARI.nextprime(1000)                  # works\nPARI.alggroupcenter(x1; x2::Union{Nothing,PariConvertible} = nothing)\n\nThe same milestone adds avma capture/restore to the ~86 scalar- and void-returning G-taking bindings and an explicit GC.@preserve region around every _trap_call. Neither changes an API; the first stops leaking every converted temporary, the second closes a rare use-after-free window.","title":"3.13 Generated-binding signatures widen (M15)"},{"category":"section","location":"api-redesign/#3.14-gp_eval's-contract-is-named-(M16)","page":"API redesign","text":"Source-compatible: the break is in the documented contract, not the signature.\n\n# before — one hidden global GP environment\ngp_eval(\"v = 42\")\ngp_eval(\"v\")\n\n# after — the same call, now documented as the default session\ngp_eval(\"v = 42\")\ngp_eval(\"v\")\n\ns = LibPARI.GPSession()\ngp_eval(s, \"v = 42\")\nLibPARI.reset!(s)","title":"3.14 gp_eval's contract is named (M16)"},{"category":"section","location":"api-redesign/#3.15-repr(g)-changes-(M18)","page":"API redesign","text":"Any test or doctest comparing repr or string output changes. print/string keep PARI's text, so interpolation is unaffected.\n\n# before\nrepr(Gen(42))          # \"42\" — indistinguishable from a String's content\nrepr(gp_eval(\"\\\"a\\\"\"))  # \"a\"\n\n# after\nrepr(Gen(42))          # a compact, self-identifying LibPARI form\nprint(Gen(42))         # 42  — unchanged\n\"$(Gen(42))\"           # \"42\" — unchanged\n\nM17 (structured objects) adds methods to a type that currently has none of them, and is expected to break nothing.\n\n","title":"3.15 repr(g) changes (M18)"},{"category":"section","location":"api-redesign/#4.-Migration-examples","page":"API redesign","text":"Runnable before/after pairs for the changes a user actually hits. (A complete per-change guide ships as the migration page in M21.)\n\nGuarding on the type. Replace Number guards with PariObject, or better, with gentype:\n\n# before\nf(x) = x isa Number ? x + 1 : error(\"not numeric\")\n\n# after\nf(x::LibPARI.Gen)  = x + 1\nf(x::Number)       = x + 1\n# or, when the PARI type matters:\nisnum(g::LibPARI.Gen) =\n    LibPARI.gentype(g) in (LibPARI.PariType.T_INT,\n                           LibPARI.PariType.T_REAL,\n                           LibPARI.PariType.T_FRAC)\n\nBuilding PARI values. Prefer the new front door; it is one name and one documented input set:\n\n# before\nusing LibPARI\ng = LibPARI.Gen(2)^LibPARI.Gen(100)\n\n# after\nusing LibPARI\ng = pari(2)^pari(100)\ng = pari(2)^100        # mixed arithmetic, explicit method\n\nFeeding a generated binding. Wrapping is no longer required:\n\n# before\nusing LibPARI\nLibPARI.PARI.nextprime(LibPARI.Gen(1000))\nLibPARI.PARI.gmodulo(LibPARI.Gen(5), LibPARI.Gen(7))\n\n# after\nusing LibPARI\nLibPARI.PARI.nextprime(1000)\nLibPARI.PARI.gmodulo(5, 7)\n\nHigh-precision reals. The lossy round-trip becomes exact, and the working precision is set with a scope:\n\n# before\nx = BigFloat(π; precision = 512)\ny = LibPARI.Gen(x)                       # crushed to 53 bits\nBigFloat(y)                              # parse of PARI's decimal text\n\n# after\nx = BigFloat(π; precision = 512)\ny = pari(x)                              # exact to 512 bits\nBigFloat(y; precision = 512) == x        # true\n\nsetprecision(Gen, 1024) do\n    LibPARI.PARI.mppi()                  # 1024-bit π\nend\n\nNote that gp_eval and show follow PARI's process-global precision, not the scope:\n\nsetprecision(Gen, 1024) do\n    gp_eval(\"Pi\")            # still the global-precision value\nend\nLibPARI.set_global_precision!(1024)   # global, thread-visible\n\nConverting out. Text parsing is gone, and the typed constructors exist:\n\n# before\nFloat64(LibPARI.Gen(1e-10))          # ArgumentError\nFloat32(LibPARI.Gen(1))              # MethodError\n\n# after\nFloat64(pari(1e-10)) === 1.0e-10     # true\nFloat32(pari(1))                     # 1.0f0\nRational{Int}(pari(1)/pari(2))       # 1//2\n\nUnsupported inputs. One named error, at the boundary:\n\n# before\nLibPARI.Gen(1) + π       # StackOverflowError — not catchable in practice\n\n# after\ntry\n    pari(1) + π\ncatch e\n    e isa LibPARI.ConversionError    # true\nend\n\nReading a container. No round trip through gp_eval:\n\n# before\nm = gp_eval(\"[1,2;3,4]\")\ngp_eval(\"[1,2;3,4][1,2]\")            # back through the parser\n\n# after\nm = gp_eval(\"[1,2;3,4]\")\nsize(m)                              # (2, 2)\nm[1, 2]                              # a fully owned Gen (element access clones)\ncollect(gp_eval(\"[1,2,3]\"))          # Vector{Gen}\n\nDisplay. If you compared repr output, compare print/string instead:\n\n# before\n@test repr(g) == \"42\"\n\n# after\n@test string(g) == \"42\"      # print/string keep PARI's text\n\n","title":"4. Migration examples"},{"category":"section","location":"api-redesign/#5.-Implementation-phases","page":"API redesign","text":"M10 (0.11.0, done)\n  └─> M11 type contract ── M12 conversion & promotion\n                                   └─> M13 precision ──┬─> M14 pari(x) + facade\n                                                       └─> M15 binding args\n        M11 ─────────────> M16 GP sessions\n        M11 ─────────────> M17 structured objects\n        M11 ─────────────> M18 display contract\n  M11+M13+M14+M17 ──────> M19 Symbolics bridge ─> M20 Giac bridge\n  all ───────────────────> M21 documentation, migration, 1.0.0\n\nID Milestone Target Depends on\nM11 Honest type contract for Gen 0.16.0 M10\nM12 Conversion & promotion contracts 0.17.0 M11\nM13 Precision-safe reals, bit-based precision API 0.18.0 M11, M12\nM14 pari(x), public surface, small facade 0.19.0 M11–M13\nM15 Generated-binding argument ergonomics 0.20.0 M12, M13\nM16 Explicit GP evaluation sessions 0.21.0 M11\nM17 Structured PARI objects 0.22.0 M11\nM18 Display contract 0.23.0 M11\nM19 Symbolics.jl bridge (optional extension) 0.24.0 M11, M13, M14, M17\nM20 Giac.jl bridge (optional extension) 0.25.0 M19\nM21 Documentation, migration & the 1.0.0 release 1.0.0 M11–M20\n\nM16, M17 and M18 are independent of each other and of M12–M15, and may be reordered freely once M11 has landed.\n\nwarning: M13 and M15 must not be in flight together\nBoth regenerate src/bindings.jl — M13 replaces the precision constants at every p/b site and removes the zero-argument Class: default records; M15 retypes the G slots and the D<G> keywords, and rewrites the avma/GC.@preserve scaffolding. Two concurrent regenerations cannot both satisfy the byte-identical reproducibility gate (NFR-04) or the \"every generated-file change traces to a gen/generate.jl diff in the same commit\" rule. Land one, regenerate, verify git diff --exit-code src/bindings.jl is clean, then start the other.\n\nTwo further sequencing rules apply throughout:\n\nTests before code. Each milestone's first deliverable is the failing @testitem that pins the observation it fixes. The observations quoted on this page come from an analysis pass at 0.15.1 that was not adversarially re-verified; the test is what turns each into a fact.\nM15 depends on M12 and M13, not on M14. If pari(x) slips, the generated layer can still widen — it needs the accepted-input set and the float policy, not the facade.\n\nM14 is also the one milestone in the sequence that must leave src/bindings.jl byte-identical to its pre-milestone content.\n\n","title":"5. Implementation phases"},{"category":"section","location":"api-redesign/#6.-Unresolved-design-questions","page":"API redesign","text":"Each question carries the current working assumption. None is settled until its milestone closes.","title":"6. Unresolved design questions"},{"category":"section","location":"api-redesign/#Type-contract-(M11)","page":"API redesign","text":"Gen(::Irrational) — refuse, or convert at a default precision? Working assumption: refuse, with an error pointing at PARI.mppi and friends. There is no lossless conversion, and PARI needs an explicit precision.\nPariNumber <: Number, a second wrapper for the numeric PARI tags — discuss, do not schedule. All 1241 generated bindings return a plain Gen, so a second type adds a conversion at every boundary and doubles the operator matrix. Implement only behind a tested consumer.\nWhich LinearAlgebra paths are bounded by T<:Number? [Gen(1) Gen(2); Gen(3) Gen(4)] * I works today. Working assumption: unknown — an explicit sweep must run before any claim is made in the docs.","title":"Type contract (M11)"},{"category":"section","location":"api-redesign/#Conversion-and-promotion-(M12)","page":"API redesign","text":"Is gen_convert a 1.0 public commitment or an internal seam? Working assumption: public. It is the extension point by which a downstream package adds its own numeric type without pirating Base.convert. If public, its name, signature and error contract freeze in M12.\nInf/NaN — reject with InexactError, or map ±Inf onto PARI's t_INFINITY? isfinite/isinf already key on PariType.T_INFINITY (src/numeric.jl:230), which makes the mapping tempting, but t_INFINITY does not participate in general PARI arithmetic. Working assumption: reject.\nGen(::Float16) / Gen(::Float32) — a t_REAL of source-matched precision, or today's widening to PARI's default? (Gen(Float16(1.5)) currently prints 19 significant digits.) Working assumption: undecided; whichever is chosen is documented and tested.\nA closed union excludes working third-party types. Rational{Int8}, UInt128 and typemax(UInt) all work today through the generic word/string path. Working assumption: the closed union is the guaranteed set, gen_convert is the documented extension point.\nComplex is value-preserving but not tag-preserving — PARI normalises a zero imaginary part, so gentype(Gen(Complex(1,0))) is T_INT. Working assumption: guarantee Complex(Gen(z)) == z in value only, never the tag.","title":"Conversion & promotion (M12)"},{"category":"section","location":"api-redesign/#Precision-(M13)","page":"API redesign","text":"precision(g) on an exact Gen — PARI's bitprecision answers +oo. Throw, return typemax(Int), or 0? Working assumption: throw ArgumentError, paired with LibPARI.isexact — it keeps the ::Int return stable, at the cost of generic code that calls precision blindly.\nScope mechanism. Julia 1.10 LTS has no Base.ScopedValues. Task-local storage nests correctly but is not inherited by @spawned children. Working assumption: ship task-local now, revisit at a 1.11 floor rather than take a ScopedValues.jl compat dependency.\nIs default_precision() 128 bits (GP parity, realbitprecision) or 256 (Julia parity with precision(BigFloat))? The two give different answers for the same expression. Working assumption: undecided — decided once, in M13, and then frozen.\nSeries precision (prototype code P) — its own scope, a separate setter, or a plain keyword default? It is a term count, not bits. Working assumption: a plain keyword default, kept separate from the bit-precision machinery.\nWhere does the hash/T_REAL fix land? Two 1024-bit reals differing beyond 53 bits hash equal today. Working assumption: fix in M11 (REQ-TYPE-09), because a precision API makes it reachable in practice.","title":"Precision (M13)"},{"category":"section","location":"api-redesign/#Public-surface-(M14)","page":"API redesign","text":"pari(::AbstractString) is deliberately left undefined. GP-parsing a string would quietly promote gp_eval to the primary API; building a t_STR is defensible. Working assumption: define neither at 1.0 — adding either later is non-breaking.\nScope of LibPARIPrimesExt. Primes.isprime(::Gen) is a clean match, but Primes.nextprime(n, i) and PARI's nextprime (\"smallest pseudoprime ≥ x\") differ in guarantee and arity. Working assumption: an optional weakdep extension, not a hard dependency, with a CI job that loads Primes.\nWhich factorization shape leads the docs — PARI's 2-column t_MAT, a Vector{Pair{Gen,Gen}}, or Primes.jl's Factorization? Working assumption: ship both factor (the t_MAT) and factors (the pairs), and lead with the pairs.\nRaise the compat floor above 1.10 so public can be written literally? That drops the current Julia LTS. Working assumption: no — keep 1.10 and guard public on VERSION >= v\"1.11\".\nLibPARI.Mod mirrors GP's spelling but reads oddly as a Julia function. Working assumption: keep the GP spelling, unexported.","title":"Public surface (M14)"},{"category":"section","location":"api-redesign/#Generated-binding-arguments-(M15)","page":"API redesign","text":"Do any scalar- or void-returning G-taking bindings retain their GEN argument in PARI global state? This is the milestone's main risk: a retained pointer into the transient stack would dangle once avma is restored. Working assumption: some may — the audit produces a generator deny-list keeping ::LibPARI.Gen, with the reason emitted into the generated header.\nShould _argptr(::AbstractFloat) honour the binding's prec keyword rather than a fixed precision? Working assumption: it follows whatever M13 sets as policy.\nAbstractVector/AbstractMatrix in a G slot — deferred past 1.0. t_VEC vs t_COL is genuinely ambiguous for a Julia Vector and should stay an explicit user choice (see M17).\nAbstractString in a G slot — rejected. It collides with the s/r prototype codes and makes f(\"x\") ambiguous between a string value and a variable name.\nShould codes L/U (today ::Integer) also accept a Gen? Symmetry says yes; a lossy Gen→long narrowing says no. Working assumption: deferred past 1.0.0.","title":"Generated-binding arguments (M15)"},{"category":"section","location":"api-redesign/#GP-sessions-(M16)","page":"API redesign","text":"The analysis pass for this milestone did not complete, so its central facts are unverified and REQ-GPS-01 is deliberately an investigation.\n\nAre truly independent GP environments reachable at all through libpari as embedded here? If not, is a variable-namespacing session an honest abstraction or a misleading one? Working assumption: unknown — investigate first, and if isolation is infeasible, document the non-isolation and assert it in a test rather than claim isolation.\nDoes a session belong to a task, a thread, or the process? Working assumption: determined by REQ-GPS-01's finding, then documented.\nShould reset! also reset PARI's global precision and variable priorities, or only user variables? Working assumption: only what the session demonstrably owns, with the cleared set written down.","title":"GP sessions (M16)"},{"category":"section","location":"api-redesign/#Structured-objects-(M17)","page":"API redesign","text":"Same caveat: the ownership and layout deliverables must be verified against PARI's headers before any accessor is written.\n\nIs T_LIST stable enough to expose, given it is mutable in PARI? Working assumption: excluded from the first pass, with the reason recorded.\nShould getindex on a T_MAT return a column (PARI's own semantics) or an element? Working assumption: the element, with x[:, j] for the column — Julia convention wins, and the column-major mapping is spelled out and tested rather than assumed.\nDoes iterate on a large vector need a chunked strategy to avoid one clone per element? Working assumption: start unchunked, and let the NFR-02/NFR-03-style allocation tests decide.","title":"Structured objects (M17)"},{"category":"section","location":"api-redesign/#Display-(M18)","page":"API redesign","text":"Should the compact form show the PARI type tag (e.g. Gen(t_MAT, …)) or a truncated value? Working assumption: the tag — it is what makes a t_STR unmistakable.\nIs a truncation threshold needed, and does it belong to show only or to text/plain as well? Every rendering is real PARI work plus a worker hop. Working assumption: a documented threshold on show, none on text/plain, which the user asked for explicitly.","title":"Display (M18)"},{"category":"section","location":"api-redesign/#Release-(M21)","page":"API redesign","text":"Does 1.0.0 freeze the generated layer's signatures too, or only the hand-written core? The generated layer changes whenever PARI's pari.desc changes. Working assumption: promise stability on the hand-written surface plus the shape of the generated one — which is the three-layer promise stated in section 2.","title":"Release (M21)"},{"category":"section","location":"#LibPARI.jl","page":"Home","text":"A Julia wrapper for the PARI/GP number-theory library.\n\nLibPARI embeds the PARI/GP C library in a Julia process and exposes it to Julia code in two complementary ways:\n\nGenerated bindings — nearly 1200 PARI functions, generated directly from PARI's own machine-readable function database (pari.desc), reachable through the LibPARI.PARI submodule. See API reference.\nA hand-written core — a Gen value type wrapping every PARI object, idiomatic numeric operators, type conversions to and from Julia numbers, a GP expression evaluator, and safe library lifecycle and error handling.","title":"LibPARI.jl"},{"category":"section","location":"#What-LibPARI-gives-you","page":"Home","text":"Gen is a PariObject, not a Julia Number. One wrapper type covers every PARI object — matrices, strings and closures included — so LibPARI declares the arithmetic explicitly rather than claiming a Number supertype: build a Gen from Julia integers, floats and rationals and use +, -, *, /, ^, and == directly, with a Julia number on either side.\nExact arithmetic of arbitrary size. PARI's bignum integers, rationals, and reals, with conversions back to Julia's BigInt and fixed-width integer types.\nThe whole of GP, one string away. gp_eval evaluates any GP-language expression — the escape hatch for functions the generated bindings do not cover.\nA safe C boundary. Every PARI error becomes a catchable PariError; a PARI longjmp never unwinds past Julia. PARI's transient stack is kept leak-free, and each Gen frees its own storage.\nParallel and thread-safe. Each Julia OS thread gets its own PARI context, so concurrent calls into the PARI library run in parallel on multiple cores — and a PARI error raised on any thread, even simultaneously from many, is caught safely as a PariError.\nType-stable and precompiled — built to SciML inference standards, with a precompilation workload for a fast first call.","title":"What LibPARI gives you"},{"category":"section","location":"#Status","page":"Home","text":"LibPARI is at version 0.16.0, the first release of the pre-1.0 API redesign. The hand-written core (lifecycle, Gen, error handling, conversions, the numeric API, precision, the pari entry point and the GP evaluator) and the generated binding layer are complete; the library is platform-correct, parallel, and thread-safe.\n\n0.16.0 carries breaking changes, two of which alter computed values — see the changelog before upgrading. The API is still being reshaped for 1.0; ROADMAP.md Part II tracks what remains.\n\nBoth LibPARI and PARI_jll are registered in the Julia General registry — a plain Pkg.add(\"LibPARI\") installs everything on Linux, macOS, and Windows. See Getting started for the install command.","title":"Status"},{"category":"section","location":"#Contents","page":"Home","text":"Pages = [\"getting-started.md\", \"api.md\"]\nDepth = 2","title":"Contents"}]
}
